---
const { linkto } = Astro.props;
---

<a href="">{linkto}</a>

<style>
  a {
    background-color: var(--accent);
    color: var(--white);
    font-family: var(--ff-cta);
    font-size: var(--fs-n);
    font-size: 14px;
    font-weight: 500;
    padding-block: 10px;
    padding-inline: 14px;
  }
  @media (min-width: 750px) {
  }
</style>
